# Edge Functions

Functions are added phase-by-phase. Shared validation, authentication, CORS, and response helpers belong in
`_shared/`; deployable functions each live in their own directory as required by the Supabase CLI.
